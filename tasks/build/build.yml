---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: {{maven},{hashicorp/terraform}}
    tag: {{3.5.0-jdk-8-alpin0e},{"light"}}

inputs:
  - name: ci-pipeline
  - name: graphql-src

outputs:
  - name: build

caches:
  - path: graphql-src/.m2


run:
  path: ci-pipeline/tasks/build/build.sh
